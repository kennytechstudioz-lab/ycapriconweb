export const metadata = {
  title: "Transactions | Dashboard",
  description: "Manage your transactions | dashboard",
};



export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
