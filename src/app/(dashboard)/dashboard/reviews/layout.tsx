export const metadata = {
  title: "Reviews | Dashboard",
  description: "Manage your reviews | dashboard",
};



export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
