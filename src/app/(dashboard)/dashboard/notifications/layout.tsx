export const metadata = {
  title: "Notifications | Dashboard",
  description: "Manage your notifications | dashboard",
};



export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
